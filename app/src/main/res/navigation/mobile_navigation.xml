<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_cargar">

    <fragment
        android:id="@+id/nav_cargar"
        android:name="com.example.trabajoprcticoobligatorio3.ui.cargar.CargarFragment"
        android:label="@string/menu_cargar"
        tools:layout="@layout/fragment_cargar" />

    <fragment
        android:id="@+id/nav_listar"
        android:name="com.example.trabajoprcticoobligatorio3.ui.listar.ListarFragment"
        android:label="@string/menu_listar"
        tools:layout="@layout/fragment_listar" />

    <fragment
        android:id="@+id/nav_salir"
        android:name="com.example.trabajoprcticoobligatorio3.ui.salir.SalirFragment"
        android:label="@string/menu_salir"
        tools:layout="@layout/fragment_salir" />

    <fragment
        android:id="@+id/nav_busqueda"
        android:name="com.example.trabajoprcticoobligatorio3.busqueda.BusquedaFragment"
        android:label="Búsqueda"
        tools:layout="@layout/fragment_busqueda">
        <action
            android:id="@+id/action_nav_busqueda_to_nav_detalle"
            app:destination="@id/nav_detalle" />
    </fragment>

    <fragment
        android:id="@+id/nav_detalle"
        android:name="com.example.trabajoprcticoobligatorio3.busqueda.DetalleFragment"
        android:label="Detalle del Producto"
        tools:layout="@layout/fragment_detalle">
        <argument
            android:name="codigo"
            app:argType="string" />
        <action
            android:id="@+id/action_nav_detalle_to_nav_busqueda"
            app:destination="@id/nav_busqueda" />
    </fragment>

</navigation>
